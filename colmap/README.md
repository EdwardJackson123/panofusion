# COLMAP placeholder

This directory is intentionally kept without the COLMAP binary bundle.

For local development or private packaging, either:

- install COLMAP and make `colmap` available on PATH, or
- place the portable executable at `colmap/bin/colmap.exe`.

Do not commit the local COLMAP binary bundle to the public repository.
