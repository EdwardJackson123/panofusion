# Backend Python Runtime

Place the packaged backend Python runtime in this directory before building local release artifacts.

The runtime files are intentionally ignored by Git. Release packages must include this directory as `resources/python` so users do not need to install Python separately.
